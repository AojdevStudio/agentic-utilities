# CodeGraph repository snapshot
# Track the main SQLite database so the project graph is versioned with the repo.
!codegraph.db

# Ignore volatile SQLite sidecars and local runtime artifacts.
*.db-wal
*.db-shm
*.lock
cache/
*.log
.dirty
